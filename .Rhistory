annotation_legend = TRUE,
legend = TRUE,
border_color = NA,
cellwidth = 3,
cellheight = 1,
fontsize_row = 10,
fontsize_col = 10,
fontsize_main = 12,
fontsize_legend = 10,
fontfamily = "Times",
filename = file,
width = 9,
height = 6)
# Add text to show the clustering
cat("\nGroup", group_num, "consensus clustering:",
paste(clustering[order_idx], collapse=" "), "\n")
cat("Number of clusters:", n_clusters, "\n")
cat("Cluster sizes:", paste(table(clustering), collapse=", "), "\n")
}
# Plot consensus matrix for each group
for(g in 1:n_groups) {
plot_coca_consensus(coca_results[[g]], g, dimension_groups[[g]])
}
cat("\nThe COCA consensus matrices show how often pairs of observations were")
cat("\nclustered together across the consensus clustering iterations.\n")
cat("Dark red indicates high consensus (often clustered together),")
cat("\nwhite indicates low consensus (rarely clustered together).\n")
# Function to plot consensus matrix with discovered clusters
plot_coca_consensus <- function(coca_result, group_num, group_dims) {
# Extract consensus matrix and cluster labels from coca result
consensus_mat <- coca_result$consensusMatrix
clustering <- coca_result$clusterLabels
# Order by consensus clustering
order_idx <- order(clustering)
consensus_ordered <- consensus_mat[order_idx, order_idx]
# Create title
title <- paste0("Group ", group_num, " (", paste0("Dim", group_dims, collapse=", "),
"): COCA Consensus Matrix (K=", coca_result$K, ")")
# Generate colors based on number of clusters
n_clusters <- length(unique(clustering))
if(n_clusters <= 3) {
my_colors <- c("#E41A1C", "#377EB8", "#4DAF4A")[1:n_clusters]
} else if(n_clusters <= 8) {
my_colors <- RColorBrewer::brewer.pal(n_clusters, "Set2")
} else {
my_colors <- colorRampPalette(RColorBrewer::brewer.pal(8, "Set2"))(n_clusters)
}
file = paste0("1f_COCAheatmap_Group",group_num,".png")
# Simple heatmap without row/column annotations to avoid the error
pheatmap(consensus_ordered,
main = title,
color = viridis(100),
breaks = seq(0, 1, length.out = 101),
cluster_rows = FALSE,
cluster_cols = FALSE,
show_rownames = FALSE,
show_colnames = FALSE,
annotation_legend = TRUE,
legend = TRUE,
border_color = NA,
cellwidth = 3,
cellheight = 1,
fontsize_row = 10,
fontsize_col = 10,
fontsize_main = 12,
fontsize_legend = 10,
fontfamily = "Times",
filename = file,
width = 6,
height = 2.75)
# Add text to show the clustering
cat("\nGroup", group_num, "consensus clustering:",
paste(clustering[order_idx], collapse=" "), "\n")
cat("Number of clusters:", n_clusters, "\n")
cat("Cluster sizes:", paste(table(clustering), collapse=", "), "\n")
}
# Plot consensus matrix for each group
for(g in 1:n_groups) {
plot_coca_consensus(coca_results[[g]], g, dimension_groups[[g]])
}
cat("\nThe COCA consensus matrices show how often pairs of observations were")
cat("\nclustered together across the consensus clustering iterations.\n")
cat("Dark red indicates high consensus (often clustered together),")
cat("\nwhite indicates low consensus (rarely clustered together).\n")
# Function to plot consensus matrix with discovered clusters
plot_coca_consensus <- function(coca_result, group_num, group_dims) {
# Extract consensus matrix and cluster labels from coca result
consensus_mat <- coca_result$consensusMatrix
clustering <- coca_result$clusterLabels
# Order by consensus clustering
order_idx <- order(clustering)
consensus_ordered <- consensus_mat[order_idx, order_idx]
# Create title
title <- paste0("Group ", group_num, " (", paste0("Dim", group_dims, collapse=", "),
"): COCA Consensus Matrix (K=", coca_result$K, ")")
# Generate colors based on number of clusters
n_clusters <- length(unique(clustering))
if(n_clusters <= 3) {
my_colors <- c("#E41A1C", "#377EB8", "#4DAF4A")[1:n_clusters]
} else if(n_clusters <= 8) {
my_colors <- RColorBrewer::brewer.pal(n_clusters, "Set2")
} else {
my_colors <- colorRampPalette(RColorBrewer::brewer.pal(8, "Set2"))(n_clusters)
}
file = paste0("1f_COCAheatmap_Group",group_num,".png")
# Simple heatmap without row/column annotations to avoid the error
pheatmap(consensus_ordered,
main = title,
color = viridis(100),
breaks = seq(0, 1, length.out = 101),
cluster_rows = FALSE,
cluster_cols = FALSE,
show_rownames = FALSE,
show_colnames = FALSE,
annotation_legend = TRUE,
legend = TRUE,
border_color = NA,
cellwidth = 3,
cellheight = 1,
fontsize_row = 10,
fontsize_col = 10,
fontsize_main = 12,
fontsize_legend = 10,
fontfamily = "Times",
filename = file,
width = 9,
height = 3)
# Add text to show the clustering
cat("\nGroup", group_num, "consensus clustering:",
paste(clustering[order_idx], collapse=" "), "\n")
cat("Number of clusters:", n_clusters, "\n")
cat("Cluster sizes:", paste(table(clustering), collapse=", "), "\n")
}
# Plot consensus matrix for each group
for(g in 1:n_groups) {
plot_coca_consensus(coca_results[[g]], g, dimension_groups[[g]])
}
cat("\nThe COCA consensus matrices show how often pairs of observations were")
cat("\nclustered together across the consensus clustering iterations.\n")
cat("Dark red indicates high consensus (often clustered together),")
cat("\nwhite indicates low consensus (rarely clustered together).\n")
# Function to plot consensus matrix with discovered clusters
plot_coca_consensus <- function(coca_result, group_num, group_dims) {
# Extract consensus matrix and cluster labels from coca result
consensus_mat <- coca_result$consensusMatrix
clustering <- coca_result$clusterLabels
# Order by consensus clustering
order_idx <- order(clustering)
consensus_ordered <- consensus_mat[order_idx, order_idx]
# Create title
title <- paste0("Group ", group_num, " (", paste0("Dim", group_dims, collapse=", "),
"): COCA Consensus Matrix (K=", coca_result$K, ")")
# Generate colors based on number of clusters
n_clusters <- length(unique(clustering))
if(n_clusters <= 3) {
my_colors <- c("#E41A1C", "#377EB8", "#4DAF4A")[1:n_clusters]
} else if(n_clusters <= 8) {
my_colors <- RColorBrewer::brewer.pal(n_clusters, "Set2")
} else {
my_colors <- colorRampPalette(RColorBrewer::brewer.pal(8, "Set2"))(n_clusters)
}
file = paste0("1f_COCAheatmap_Group",group_num,".png")
# Simple heatmap without row/column annotations to avoid the error
pheatmap(consensus_ordered,
main = title,
color = viridis(100),
breaks = seq(0, 1, length.out = 101),
cluster_rows = FALSE,
cluster_cols = FALSE,
show_rownames = FALSE,
show_colnames = FALSE,
annotation_legend = TRUE,
legend = TRUE,
border_color = NA,
cellwidth = 3,
cellheight = 1,
fontsize_row = 10,
fontsize_col = 10,
fontsize_main = 12,
fontsize_legend = 10,
fontfamily = "Times",
filename = file,
width = 8,
height = 3)
# Add text to show the clustering
cat("\nGroup", group_num, "consensus clustering:",
paste(clustering[order_idx], collapse=" "), "\n")
cat("Number of clusters:", n_clusters, "\n")
cat("Cluster sizes:", paste(table(clustering), collapse=", "), "\n")
}
# Plot consensus matrix for each group
for(g in 1:n_groups) {
plot_coca_consensus(coca_results[[g]], g, dimension_groups[[g]])
}
cat("\nThe COCA consensus matrices show how often pairs of observations were")
cat("\nclustered together across the consensus clustering iterations.\n")
cat("Dark red indicates high consensus (often clustered together),")
cat("\nwhite indicates low consensus (rarely clustered together).\n")
# Function to plot consensus matrix with discovered clusters
plot_coca_consensus <- function(coca_result, group_num, group_dims) {
# Extract consensus matrix and cluster labels from coca result
consensus_mat <- coca_result$consensusMatrix
clustering <- coca_result$clusterLabels
# Order by consensus clustering
order_idx <- order(clustering)
consensus_ordered <- consensus_mat[order_idx, order_idx]
# Create title
title <- paste0("Group ", group_num, " (", paste0("Dim", group_dims, collapse=", "),
"): COCA Consensus Matrix (K=", coca_result$K, ")")
# Generate colors based on number of clusters
n_clusters <- length(unique(clustering))
if(n_clusters <= 3) {
my_colors <- c("#E41A1C", "#377EB8", "#4DAF4A")[1:n_clusters]
} else if(n_clusters <= 8) {
my_colors <- RColorBrewer::brewer.pal(n_clusters, "Set2")
} else {
my_colors <- colorRampPalette(RColorBrewer::brewer.pal(8, "Set2"))(n_clusters)
}
file = paste0("1f_COCAheatmap_Group",group_num,".png")
# Simple heatmap without row/column annotations to avoid the error
pheatmap(consensus_ordered,
main = title,
color = viridis(100),
breaks = seq(0, 1, length.out = 101),
cluster_rows = FALSE,
cluster_cols = FALSE,
show_rownames = FALSE,
show_colnames = FALSE,
annotation_legend = TRUE,
legend = TRUE,
border_color = NA,
cellwidth = 3,
cellheight = 3,
fontsize_row = 10,
fontsize_col = 10,
fontsize_main = 12,
fontsize_legend = 10,
fontfamily = "Times",
filename = file,
width = 7,
height = 3)
# Add text to show the clustering
cat("\nGroup", group_num, "consensus clustering:",
paste(clustering[order_idx], collapse=" "), "\n")
cat("Number of clusters:", n_clusters, "\n")
cat("Cluster sizes:", paste(table(clustering), collapse=", "), "\n")
}
# Plot consensus matrix for each group
for(g in 1:n_groups) {
plot_coca_consensus(coca_results[[g]], g, dimension_groups[[g]])
}
cat("\nThe COCA consensus matrices show how often pairs of observations were")
cat("\nclustered together across the consensus clustering iterations.\n")
cat("Dark red indicates high consensus (often clustered together),")
cat("\nwhite indicates low consensus (rarely clustered together).\n")
# Function to plot consensus matrix with discovered clusters
plot_coca_consensus <- function(coca_result, group_num, group_dims) {
# Extract consensus matrix and cluster labels from coca result
consensus_mat <- coca_result$consensusMatrix
clustering <- coca_result$clusterLabels
# Order by consensus clustering
order_idx <- order(clustering)
consensus_ordered <- consensus_mat[order_idx, order_idx]
# Create title
title <- paste0("Group ", group_num, " (", paste0("Dim", group_dims, collapse=", "),
"): COCA Consensus Matrix (K=", coca_result$K, ")")
# Generate colors based on number of clusters
n_clusters <- length(unique(clustering))
if(n_clusters <= 3) {
my_colors <- c("#E41A1C", "#377EB8", "#4DAF4A")[1:n_clusters]
} else if(n_clusters <= 8) {
my_colors <- RColorBrewer::brewer.pal(n_clusters, "Set2")
} else {
my_colors <- colorRampPalette(RColorBrewer::brewer.pal(8, "Set2"))(n_clusters)
}
file = paste0("1f_COCAheatmap_Group",group_num,".png")
# Simple heatmap without row/column annotations to avoid the error
pheatmap(consensus_ordered,
main = title,
color = viridis(100),
breaks = seq(0, 1, length.out = 101),
cluster_rows = FALSE,
cluster_cols = FALSE,
show_rownames = FALSE,
show_colnames = FALSE,
annotation_legend = TRUE,
legend = TRUE,
border_color = NA,
cellwidth = 9,
cellheight = 3,
fontsize_row = 10,
fontsize_col = 10,
fontsize_main = 12,
fontsize_legend = 10,
fontfamily = "Times",
filename = file,
width = 7,
height = 3)
# Add text to show the clustering
cat("\nGroup", group_num, "consensus clustering:",
paste(clustering[order_idx], collapse=" "), "\n")
cat("Number of clusters:", n_clusters, "\n")
cat("Cluster sizes:", paste(table(clustering), collapse=", "), "\n")
}
# Plot consensus matrix for each group
for(g in 1:n_groups) {
plot_coca_consensus(coca_results[[g]], g, dimension_groups[[g]])
}
cat("\nThe COCA consensus matrices show how often pairs of observations were")
cat("\nclustered together across the consensus clustering iterations.\n")
cat("Dark red indicates high consensus (often clustered together),")
cat("\nwhite indicates low consensus (rarely clustered together).\n")
rm(list = ls())
load("~/Documents/MPhil Population Health Sciences/Dissertation/thesis/leap/results/leap_multiCOCA2.RData")
table(classifications2$Group1)
table(classifications2$Group2)
table(classifications2$Group3)
table(classifications2$Group4)
table(classifications2$Group45)
table(classifications2$Group5)
table(classifications2$Group6)
table(classifications2$Group7)
table(classifications2$Group8)
table(classifications2$Group9)
load("~/Documents/MPhil Population Health Sciences/Dissertation/thesis/leap/data/leap_views.RData")
# Run in parallel
moc_list <- list()
for (d in 1:length(data_group)) {
moc_list[[d]] <- constructMOC(data_group[[d]])
names(moc_list)[d] <- paste0("Group", d)
}
# LEAP MultiCOCA
# Set file directory
setwd("~/Documents/MPhil Population Health Sciences/Dissertation/thesis")
# Load functions
source("r functions/constructMOC.R")
source("r functions/numCores.R")
source("r functions/multicoca.R")
# Load packages
library(doParallel)
library(foreach)
# Run in parallel
moc_list <- list()
for (d in 1:length(data_group)) {
moc_list[[d]] <- constructMOC(data_group[[d]])
names(moc_list)[d] <- paste0("Group", d)
}
for (moc in moc_list) { }
for (MOC in moc_list) { cat("- Sparsity:", round(sum(MOC == 0) / length(MOC) * 100, 1), "% zeros\n") }
table(data_group$Group1)
unique(data_group[[1]])
for (i in length(data_group)) {
n_clust <- as.numeric(data_group[[i]])
table(unique(n_clust))
}
n_clust <- as.numeric(data_group[[i]])
for (i in length(data_group)) {
n_clust <- as.numeric(unique(data_group[[i]]))
table(unique(n_clust))
}
n_clust <- unique(data_group[[i]])
i <- 1
clust <- data_group[[i]]
length(clust)
clust[1]
clust[1]
clust <- data_group[[i]]
n_clust <- numeric())
clust <- data_group[[i]]
n_clust <- numeric()
for (j in length(clust)) {
n_clust[j] <- unique(clust[i])
}
n_clust2 <- unique(clust[i])
as.numeric(clust[1])
as.numeric(clust)[1]
unique(clust)[1]
clust[1]
clust2 <- clust[1]
clust2 <- as.numeric(clust[1])
clust <- data_group[[i]]
clust <- as.data.frame(clust)
n_clust <- numeric()
for (j in length(clust)) {
clust2 <- as.numeric(clust[1])
}
clust2 <- as.numeric(clust[1])
clust[1]
as.numeric(clust)[1]
clust2 <- as.numeric(clust[[1]])
clust <- data_group[[i]]
clust <- as.data.frame(clust)
n_clust <- numeric()
for (j in length(clust)) {
clust2 <- as.numeric(clust[[j]])
n_clusts <- unique(clust2)
n_clust[j] <- n_clusts
}
clust <- data_group[[i]]
clust <- as.data.frame(clust)
n_clust <- numeric()
for (j in length(clust)) {
clust2 <- as.numeric(clust[[j]])
n_clusts <- unique(clust2)
n_clust <- append(n_clust, n_clusts)
}
clust <- data_group[[i]]
clust <- as.data.frame(clust)
n_clust <- numeric()
length(clust)
for (j in length(clust)) {
clust2 <- as.numeric(clust[[j]])
n_clusts <- unique(clust2)
n_clust <- append(n_clust, n_clusts)
}
n_clusts <- unique(clust2)
clust <- data_group[[i]]
clust <- as.data.frame(clust)
n_clust <- numeric()
for (j in 1:length(clust)) {
clust2 <- as.numeric(clust[[j]])
n_clusts <- unique(clust2)
n_clust <- append(n_clust, n_clusts)
}
for (i in 1:length(data_group)) {
clust <- data_group[[i]]
clust <- as.data.frame(clust)
n_clust <- numeric()
for (j in 1:length(clust)) {
clust2 <- as.numeric(clust[[j]])
n_clusts <- unique(clust2)
n_clust <- append(n_clust, n_clusts)
}
print(paste0("Group",i," Number of Clusters"))
print(table(n_clust))
}
clust <- data_group[[i]]
clust <- as.data.frame(clust)
n_clust <- numeric()
length(clust)
i <- 1
clust <- data_group[[i]]
clust <- as.data.frame(clust)
n_clust <- numeric()
clust2 <- as.numeric(clust[[j]])
n_clusts <- unique(clust2)
for (i in 1:length(data_group)) {
clust <- data_group[[i]]
clust <- as.data.frame(clust)
n_clust <- numeric()
for (j in 1:length(clust)) {
clust2 <- as.numeric(clust[[j]])
n_clusts <- length(unique(clust2))
n_clust <- append(n_clust, n_clusts)
}
print(paste0("Group",i," Number of Clusters"))
print(table(n_clust))
}
moc_list[[2]]
moc_list[[1]]
for (MOC in moc_list) {
print(paste("Group",MOC))
cat("- Sparsity:", round(sum(MOC == 0) / length(MOC) * 100, 1), "% zeros\n")
}
for (i in 1:length(data_group)) {
clust <- data_group[[i]]
clust <- as.data.frame(clust)
n_clust <- numeric()
for (j in 1:length(clust)) {
clust2 <- as.numeric(clust[[j]])
n_clusts <- length(unique(clust2))
n_clust <- append(n_clust, n_clusts)
}
print("\nNumber of Proteins: ", nrow(clust))
print(paste0("Group",i," Number of Clusters"))
print(table(n_clust))
}
for (i in 1:length(data_group)) {
clust <- data_group[[i]]
clust <- as.data.frame(clust)
n_clust <- numeric()
for (j in 1:length(clust)) {
clust2 <- as.numeric(clust[[j]])
n_clusts <- length(unique(clust2))
n_clust <- append(n_clust, n_clusts)
}
print("\nNumber of Proteins: ", length(clust))
print(paste0("Group",i," Number of Clusters"))
print(table(n_clust))
}
for (i in 1:length(data_group)) {
clust <- data_group[[i]]
clust <- as.data.frame(clust)
n_clust <- numeric()
for (j in 1:length(clust)) {
clust2 <- as.numeric(clust[[j]])
n_clusts <- length(unique(clust2))
n_clust <- append(n_clust, n_clusts)
}
print(paste0("\nNumber of Proteins: ", length(clust)))
print(paste0("Group",i," Number of Clusters"))
print(table(n_clust))
}
for (MOC in moc_list) {
cat("- Sparsity:", round(sum(MOC == 0) / length(MOC) * 100, 1), "% zeros\n")
}
for (moc in moc_list) {print(length(moc))}
for (moc in moc_list) {print(length(moc)/419)}
table(classifications$Group2)
table(classifications2$Group2)
table(classifications2$Group3)
table(classifications2$Group1)
